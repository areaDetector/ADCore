This directory contains the libxml2 code (2.9.4).  It has been slightly modified to build under the EPICS build system.

It is intended for use on WIN32, vxWorks, and RTEMS.  On Linux and OS-X the standard package managers should be used instead.

These are the files that have been modified from the original distribution

Makefile  This is completely new

